# Chronic Kidney Disease Prediction – ML Module

This module builds and evaluates a machine learning model to predict
Chronic Kidney Disease using Random Forest and SelectKBest.

## Steps to Run

1. Install dependencies
   pip install -r requirements.txt

2. Train the model
   python src/train_model.py

3. Evaluate the model
   python src/evaluate_model.py

## Tech Used
- Python
- scikit-learn
- Random Forest
- SelectKBest
